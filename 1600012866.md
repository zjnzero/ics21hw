### homework1
#### Ex2.61

```c
boot check_bytes = !(x - 0xffffffff) | !x | !((x & 0xff) - 0xff) | !(x >> ((sizeof(int) - 1) << 3) & 0xff);
// 暴力做法，每一次都把满足要求的对应bit转化为0，用逻辑非运算来实现判断
```

#### Ex2.62

```c
bool int_shifts_are_arithmetic(){
	return !((-1 >> 1) ^ INT_MAX);
	// 算术右移的情况下，-1>>1是INT_MAX，与自身进行Xor操作得0
}
```

#### Ex2.65

```c
/* Return 1 when x contains an odd number of 1s; 0 otherwise.
   Assume w=32. The function cantains less than 12 operations. */
int odd_ones(unsigned x){
	x ^= (x >> 16);
	x ^= (x >> 8);
	x ^= (x >> 4);
	x ^= (x >> 2);
	x ^= (x >> 1);
	return x & 1;
	// 由于Xor操作的性质，不会改变操作数的奇偶性
	// 用二分方法，每一次操作都把高(len/2)位的奇偶性加载到低(len/2)上
}
```
